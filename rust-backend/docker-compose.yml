version: '3.8'

services:
  rust-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-drive-rust-backend
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env
